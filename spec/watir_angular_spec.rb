require "spec_helper"

RSpec.describe WatirAngular do
  it "has a version number" do
    expect(WatirAngular::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
